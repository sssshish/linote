/* eslint-disable prettier/prettier */
import * as React from 'react';
import { View, Text } from "react-native";

export const Wallet = () => {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Categories and wallets here!</Text>
        </View>
    );
};
